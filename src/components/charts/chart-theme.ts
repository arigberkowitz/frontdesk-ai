/**
 * Deprecated: chart tokens now live in ./theme.ts (single source of truth).
 * Kept as a re-export so any stray import keeps working.
 */
export { CHART_COLORS, CHART_GRID, CHART_AXIS, CHART_AXIS_FONT_SIZE, TOOLTIP_STYLE } from "./theme";
